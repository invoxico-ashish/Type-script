const hero: string[] = [];
// const heroPower:number[] = []
const heroPower: Array<number> = [];

type User = {
    name: string
    isActive: boolean
}
const allUsers: User[] = [];
allUsers.push({ name: "", isActive: true });

const MLModels: number[][] = [  // array inside an arrayk
    [255, 255, 255]
]

