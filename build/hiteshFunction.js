"use strict";
function getVal(myVal) {
    if (myVal > 5) {
        return true;
    }
    ;
    return "200 OK";
}
;
const getHello = (s) => {
    return ``;
};
const heros = ["thor", "CA", "spiderMan"];
heros.map((hero) => {
    return `hero is ${hero}`;
});
function consoleError(err) {
    console.log(err);
}
;
function handleError(err) {
    throw new Error(err);
}
;
